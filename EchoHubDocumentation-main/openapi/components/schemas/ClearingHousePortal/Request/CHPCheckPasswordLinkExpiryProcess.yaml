required:
  - RequestValueType
  - RequestValue
type: object
properties:
 
  APIFormIdentifierId: 
    type: integer
    description: The APIFormIdentifierID is 45. This is a unique identifier that serves the purpose of identifying a particular request. It helps identify and map the request to the correct endpoint.

  Key: 
    type: string
    description: when a clearinghouse user request is created, a unique URL is sent to the clearinghouse admin via email to complete the registration process which will have a key as part of the unique URL.
    maxLength: 100
    minLength: 4
  
    